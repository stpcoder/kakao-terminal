interface:
  display_name: "Kakao Terminal"
  short_description: "Operate KakaoTalk on macOS from one skill."
  default_prompt: "Use $kakao-terminal to browse KakaoTalk rooms, read recent messages, or verify macOS setup."

policy:
  allow_implicit_invocation: true
